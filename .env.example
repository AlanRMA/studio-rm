# Variáveis do Next.js (Vercel → Settings → Environment Variables)
# Use os mesmos valores em Production. Preview pode apontar para backend dev.

# ── LOCAL ──────────────────────────────────────────────────────────────────
RECEIPT_API_URL=http://localhost:4000
RECEIPT_API_KEY=RosaniaChaveApi

# ── VERCEL PRODUCTION (após deploy no Render) ──────────────────────────────
# RECEIPT_API_URL=https://receipt-backend-rosania.onrender.com
# RECEIPT_API_KEY=mesma-chave-do-INGEST_API_KEY-no-Render